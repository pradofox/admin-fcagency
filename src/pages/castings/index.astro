---
import { requireUser } from '../../lib/session';

// Castings ya no es una sección propia: se administra desde el perfil de cada
// cliente (alta y procesos del casting). Esta ruta queda solo por
// compatibilidad de links viejos y redirige a Clientes.
requireUser(Astro);
return Astro.redirect('/clientes');
---
