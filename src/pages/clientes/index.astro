---
import Layout from '../../components/Layout.astro';
import EmptyState from '../../components/EmptyState.astro';
import { requireUser } from '../../lib/session';
requireUser(Astro);
---

<Layout title="Clientes" pageTitle="Clientes">
  <div class="page-head"><h1>Clientes</h1></div>
  <EmptyState
    title="Próximamente"
    message="Esta sección se construye en la Fase 2. Catálogo de clientes con historial de producciones y notas. Se alimenta también de la sección Contactos."
  />
</Layout>
