-- Link al perfil publico de la modelo (sitio web de FC Agency o portfolio).
-- Permite tener acceso rapido desde admin al perfil visible para clientes.
ALTER TABLE modelos ADD COLUMN link_perfil_web TEXT;
